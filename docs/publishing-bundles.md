# Publishing bundles (v0.9)

- Route: `/publishing`.
- Bundle outputs include deterministic manifest + summary JSON.
- Supports explicit bundle types, source context, included items, provenance summary, and integrity hash.
