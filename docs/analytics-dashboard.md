# Analytics dashboard (v0.8)

- Route: `/analytics`.
- Uses local deterministic summaries and filter rules.
- Avoids synthetic trend lines when no real series exists.
- Exports can be derived from analytics summary/pipeline manifests.
