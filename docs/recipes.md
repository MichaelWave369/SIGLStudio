# Automation recipes (v0.8)

- Route: `/recipes`.
- Recipes are local-first, explicit, and user-triggered.
- Recipe runs emit deterministic run summaries (`recipe_id`, `action_count`, `completed_at`).
- No background job scheduler or hidden automation.
