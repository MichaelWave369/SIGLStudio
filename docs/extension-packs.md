# Extension packs (v0.8)

- Extension packs include explicit `schema_version` fields.
- Imports run migration-aware validation before accepting data.
- Strategy remains config/data-only, no arbitrary code execution.
