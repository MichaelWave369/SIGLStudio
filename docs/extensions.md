# Local extension hooks (v0.7)

- Extensions are local-only config entries validated by schema (`lib/extensionTypes.ts`).
- Current hook categories: review-template-pack, artifact-preset, handoff-formatter, board-starter, analytics-grouping.
- Registry import ignores invalid entries and surfaces validation errors.
